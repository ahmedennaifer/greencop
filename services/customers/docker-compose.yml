
services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: greencop
      POSTGRES_USER: nafra
      POSTGRES_PASSWORD: test
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nafra -d greencop"]
      interval: 5s
      timeout: 5s
      retries: 5

  customers-api:
    build: .
    ports:
      - "8081:8080"
    depends_on:
      postgres:
        condition: service_healthy
    env_file: .env
    volumes:
      - ~/.config/gcloud:/home/app/.config/gcloud:ro
    environment:
      - GOOGLE_APPLICATION_CREDENTIALS=/home/app/.config/gcloud/application_default_credentials.json
    command: >
      sh -c "
        cd customers &&
        cd .. &&
        uvicorn main:app --host 0.0.0.0 --port 8080
      "

volumes:
  postgres_data:
